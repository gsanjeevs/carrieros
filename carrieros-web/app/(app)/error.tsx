'use client'

// Boundary inside the authenticated shell: the sidebar/layout stays mounted
// and only the failed page area is replaced.
import ErrorView from '../error-view'

export default ErrorView
